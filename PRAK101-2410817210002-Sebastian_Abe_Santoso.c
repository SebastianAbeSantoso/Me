#include <stdio.h>
int main(){
    int  x = 5, y = 7, z = 9, total = x + y + z;
    printf("Variabel x bernilai %d\nVariabel y bernilai %d\nVariabel z bernilai %d\nJumlah variabel tersebut adalah %d", x, y, z, total);
    return 0;
}